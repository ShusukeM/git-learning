実際の開発をするときは、stageに追加する前（addする前）やコミットする前に
git diff / git diff --staged　+ git statusを確認する
git log --oneline
git log - p index.html
git log -n <commit limitation num>

git rm file_name
git rm -r dir_name
git rm -cached file_name(worktreeに残しておくが、git repositoryからは削除)

<rmを戻すには以下の2行（cachedの場合は一つ目だけ＞
git reset HEAD file_name
git checkout file_name